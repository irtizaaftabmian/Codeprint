Launch the interactive codeprint browser graph for the current directory.

```bash
codeprint &
```

Tell the user the graph is opening at http://localhost:3456 (or the next available port). They can explore the dependency map, filter by category, find dead code, and open files directly in their editor.
